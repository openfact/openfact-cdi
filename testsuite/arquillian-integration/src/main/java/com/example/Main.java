package com.example;

public class Main {

    public static void main(String[] args) throws Exception {
        //Swarm container = new Swarm();

        /*container.fraction(new JAXRSFraction());
        container.fraction(new CDIFraction());
        container.fraction(EJBFraction.createDefaultFraction());*/
        
        //WARArchive deployment = ShrinkWrap.create(WARArchive.class);                
       /* deployment.addClass(RestApplication.class);
        deployment.addClass(HelloWorldEndpoint.class);
        deployment.addClass(EmployeeService.class);
        deployment.addClass(Calculadora.class);
        //deployment.addAllDependencies();
        deployment.addAsManifestResource(EmptyAsset.INSTANCE, "beans.xml");
        */
        /*container.start();
        container.deploy();*/
    }
}
